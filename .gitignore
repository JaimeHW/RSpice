# Rust
# Unanchored: the workspace root is not the only crate root. The reference
# probes under benchmarks/, agent worktrees, and crates/rspice-core all grow
# their own target/ directory.
target/

# Standalone benchmark probes are throwaway harnesses rebuilt on demand, so
# their lockfiles are not part of the reproducible build. Drop this rule if a
# probe result ever needs to be reproduced exactly.
benchmarks/reference/*/Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node (for Tauri web frontend)
node_modules/
dist/
.vite/

# Separately versioned local prototypes
/mockups/

# Upstream checkouts backing models/spice (repopulated by tools/models/sync_packs.py)
/.model-src-cache/

# Tauri
src-tauri/target/

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.exe
*.dll
*.so
*.dylib

# Python tooling caches and environments.
#
# uv, mypy, and pytest each write their own .gitignore inside these, so they
# are already excluded in practice. Listing them anyway means `git status`
# stays honest if a tool ever stops doing that, and it documents that the
# virtualenv inside crates/rspice-python is deliberate: maturin develop needs
# an active venv, and the crate-local one is what the build instructions use.
.venv/
.mypy_cache/
.pytest_cache/

# Test outputs
*.raw
!tests/xyce/OutputData/Output/DC/*.raw
*.log
*.pyc
__pycache__/
/*.out
*.osdi
*.zip
/rtlinv_*.csv
/rtlinv_*.json
/rtlinv_*.cir
/diagnostics/
/design/
/docs/
/.codex-tools/
/tmp/
.agent/
.worktrees/
/_site
/_site-*/
/_site-source/
