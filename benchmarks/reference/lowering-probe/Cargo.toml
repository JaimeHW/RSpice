[workspace]

[package]
name = "lowering-probe"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "lowering-probe"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
