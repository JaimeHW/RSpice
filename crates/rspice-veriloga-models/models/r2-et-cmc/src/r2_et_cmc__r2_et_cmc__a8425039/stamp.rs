// @generated by rspice-veriloga; do not edit.
#![allow(dead_code, non_snake_case, unused_imports, unused_mut, unused_parens, unused_variables)]

use super::state::{CanonicalModelValues, Instance, PARAMETER_MODEL_FLAGS};
use rspice_veriloga_runtime::{GeneratedEvalContext, GeneratedReactiveStamper, GeneratedStamper, install_generated_stage_values, L2, L3, evaluate_generated_above, evaluate_generated_cross, evaluate_generated_timer, rspice_eval_ddt, rspice_eval_idt, rspice_limexp, rspice_limited_exp, rspice_limited_exp_derivative};
use std::collections::HashMap;
use std::sync::{Arc, Mutex, OnceLock, Weak};
pub(super) const CANONICAL_MODEL_STAGE_SLOTS: [u32; 4] = [0, 3, 8, 2];
pub(super) const CANONICAL_INSTANCE_STAGE_SLOTS: [u32; 29] = [13, 14, 15, 16, 18, 17, 19, 21, 22, 20, 23, 24, 25, 9, 6, 26, 28, 27, 29, 11, 12, 5, 4, 7, 30, 10, 31, 32, 33];
pub(super) const CANONICAL_TEMPERATURE_STAGE_SLOTS: [u32; 1] = [1];

pub(super) fn canonical_model_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 4] {
	let A=parameter_given[10] as u8 as f64;
	let B=parameters[10];
	let C=1f64;
	let E=parameter_given[11] as u8 as f64;
	let G=1f64;
	let D=if parameter_given[10]{
	B
	}else{
	C
	};
	let H=if parameter_given[11]{
	let F=C- (0.01f64* parameters[11]);
	F
	}else{
	G
	};
	let I=(H* D)* 1e6f64;
	let J=273.15f64+ parameters[16];
	let K=(parameters[29]> 0f64)|| (parameters[27]> 0f64);
	let L=parameters[35]+ C;
    [I, J, K as u8 as f64, L]
}

pub(super) fn canonical_instance_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 29] {
	let A=parameters[3];
	let B=parameters[4];
	let D=parameters[23];
	let G=parameter_given[1] as u8 as f64;
	let H=parameter_given[2] as u8 as f64;
	let J=0.5f64;
	let L=0f64;
	let N=parameters[2];
	let O=parameters[1];
	let X=parameters[0];
	let Y=staged[0];
	let AA=parameters[22];
	let AC=1e99f64;
	let AM=parameters[17];
	let DK=parameters[25];
	let DL=parameters[24];
	let DR=parameters[37];
	let DS=parameters[38];
	let DW=parameters[39];
	let DY=parameters[40];
	let mut oE=false;
	let mut oP=false;
	let mut oQ=false;
	let mut oAL=false;
	let mut oAW=false;
	let mut oAX=false;
	let mut oBG=false;
	let mut oBX=false;
	let mut oCJ=false;
	let mut oDC=false;
	let mut oDF=false;
	let mut oEA=false;
	let mut oEM=false;
	let mut oEV=0.0;
	let mut oEY=false;
	let C=A!=0.0&& B!=0.0;
	let F;
	if C{
	F=D;
	}else{
	let E=A!=0.0|| B!=0.0;
	oE=E;
	let M=if E{
	let K=D* J;
	K
	}else{
	L
	};
	F=M;
	}
	let I=(parameter_given[1]&& parameter_given[2])&& (!(parameter_given[0]));
	let R;
	let S;
	let T;
	let U;
	let V;
	let W;
	if I{
	let P=(N== L)|| (O== L);
	oP=P;
	let AF;
	let AG;
	let AH;
	let AI;
	let AJ;
	let AK;
	if P{
	let Z=X* Y;
	let AB=Z+ AA;
	AF=L;
	AG=Z;
	AH=L;
	AI=L;
	AJ=AB;
	AK=AC;
	}else{
	let AD=O* Y;
	let AE=AD+ F;
	let AL=AE> L;
	oAL=AL;
	let AR;
	let AS;
	let AT;
	let AU;
	if AL{
	let AN=(AM/ N)* AE;
	let AO=AN- AA;
	let AV=1f64/ N;
	AR=AO;
	AS=N;
	AT=AN;
	AU=AV;
	}else{
	let AP=X* Y;
	let AQ=AP+ AA;
	AR=AP;
	AS=L;
	AT=AQ;
	AU=AC;
	}
	AF=AD;
	AG=AR;
	AH=AE;
	AI=AS;
	AJ=AT;
	AK=AU;
	}
	R=AF;
	S=AG;
	T=AH;
	U=AI;
	V=AJ;
	W=AK;
	}else{
	let Q=parameter_given[2]&& (!(parameter_given[1]));
	oQ=Q;
	let AY;
	let AZ;
	let BA;
	let BB;
	let BC;
	let BD;
	if Q{
	let AW=N== L;
	oAW=AW;
	let BH;
	let BI;
	let BJ;
	let BK;
	let BL;
	let BM;
	if AW{
	let BE=X* Y;
	let BF=BE+ AA;
	BH=L;
	BI=BE;
	BJ=L;
	BK=L;
	BL=BF;
	BM=AC;
	}else{
	let BG=X== L;
	oBG=BG;
	let BR;
	let BS;
	let BT;
	let BU;
	let BV;
	let BW;
	if BG{
	let BN=O* Y;
	let BO=BN+ F;
	BR=BN;
	BS=L;
	BT=BO;
	BU=AC;
	BV=L;
	BW=L;
	}else{
	let BP=X* Y;
	let BQ=BP+ AA;
	let BX=BQ> L;
	oBX=BX;
	let CC;
	let CD;
	let CE;
	let CF;
	if BX{
	let BY=(N/ AM)* BQ;
	let BZ=BY- F;
	let CG=1f64/ N;
	CC=BZ;
	CD=BY;
	CE=N;
	CF=CG;
	}else{
	let CA=O* Y;
	let CB=CA+ F;
	CC=CA;
	CD=CB;
	CE=AC;
	CF=L;
	}
	BR=CC;
	BS=BP;
	BT=CD;
	BU=CE;
	BV=BQ;
	BW=CF;
	}
	BH=BR;
	BI=BS;
	BJ=BT;
	BK=BU;
	BL=BV;
	BM=BW;
	}
	AY=BH;
	AZ=BI;
	BA=BJ;
	BB=BK;
	BC=BL;
	BD=BM;
	}else{
	let AX=X== L;
	oAX=AX;
	let CK;
	let CL;
	let CM;
	let CN;
	let CO;
	let CP;
	if AX{
	let CH=O* Y;
	let CI=CH+ F;
	CK=CH;
	CL=L;
	CM=CI;
	CN=AC;
	CO=L;
	CP=L;
	}else{
	let CJ=O== L;
	oCJ=CJ;
	let CU;
	let CV;
	let CW;
	let CX;
	let CY;
	let CZ;
	if CJ{
	let CQ=X* Y;
	let CR=CQ+ AA;
	CU=L;
	CV=CQ;
	CW=L;
	CX=L;
	CY=CR;
	CZ=AC;
	}else{
	let CS=X* Y;
	let CT=CS+ AA;
	let DA=O* Y;
	let DB=DA+ F;
	let DC=CT> L;
	oDC=DC;
	let DD;
	let DE;
	if DC{
	let DF=DB> L;
	oDF=DF;
	let DI;
	let DJ;
	if DF{
	let DG=AM* (DB/ CT);
	let DH=1f64/ DG;
	DI=DG;
	DJ=DH;
	}else{
	DI=L;
	DJ=AC;
	}
	DD=DI;
	DE=DJ;
	}else{
	DD=AC;
	DE=L;
	}
	CU=DA;
	CV=CS;
	CW=DB;
	CX=DD;
	CY=CT;
	CZ=DE;
	}
	CK=CU;
	CL=CV;
	CM=CW;
	CN=CX;
	CO=CY;
	CP=CZ;
	}
	AY=CK;
	AZ=CL;
	BA=CM;
	BB=CN;
	BC=CO;
	BD=CP;
	}
	R=AY;
	S=AZ;
	T=BA;
	U=BB;
	V=BC;
	W=BD;
	}
	let DO=if DK!=0.0{
	let DM=T+ DL;
	DM
	}else{
	let DN=R+ DL;
	DN
	};
	let DP=U> L;
	let DQ=T> L;
	let DT;
	let DU;
	if DQ{
	let EB;
	let EC;
	if C{
	let DX=DR+ (DW/ T);
	let DZ=DS+ (DY/ T);
	EB=DX;
	EC=DZ;
	}else{
	let EA=A!=0.0|| B!=0.0;
	oEA=EA;
	let EF;
	let EG;
	if EA{
	let ED=DR+ ((J* DW)/ T);
	let EE=DS+ ((J* DY)/ T);
	EF=ED;
	EG=EE;
	}else{
	EF=DR;
	EG=DS;
	}
	EB=EF;
	EC=EG;
	}
	DT=EB;
	DU=EC;
	}else{
	DT=DR;
	DU=DS;
	}
	let DV=V> L;
	let EJ;
	let EK;
	if DV{
	let EH=DT+ (parameters[41]/ V);
	let EI=DU+ (parameters[42]/ V);
	EJ=EH;
	EK=EI;
	}else{
	EJ=DT;
	EK=DU;
	}
	let EN;
	if C{
	let EL=2f64* (R+ S);
	EN=EL;
	}else{
	let EM=A!=0.0|| B!=0.0;
	oEM=EM;
	let ET=if EM{
	let ER=(2f64* R)+ S;
	ER
	}else{
	let ES=2f64* R;
	ES
	};
	EN=ET;
	}
	let EO=R* S;
	let EP=(parameters[44]+ (parameters[45]* EN))+ (parameters[46]* EO);
	let EQ=(parameters[47]+ (parameters[48]* EN))+ (parameters[49]* EO);
	let EU=DP&& (staged[8]!=0.0);
	if EU{
	let EV=(1f64- parameters[29])- parameters[27];
	oEV=EV;
	}
	let EW=W> L;
	let EX=(parameters[6]!=0.0&& DP)&& EW;
	if EX{
	let EY=(parameters[33]!=0.0&& DQ)&& DV;
	oEY=EY;
	}
	let EZ=DP&& EW;
    [C as u8 as f64, oE as u8 as f64, I as u8 as f64, oP as u8 as f64, oAL as u8 as f64, oQ as u8 as f64, oAW as u8 as f64, oBG as u8 as f64, oBX as u8 as f64, oAX as u8 as f64, oCJ as u8 as f64, oDC as u8 as f64, oDF as u8 as f64, DO, U, DQ as u8 as f64, oEA as u8 as f64, DV as u8 as f64, oEM as u8 as f64, EP, EQ, EJ, EK, W, EU as u8 as f64, oEV, EX as u8 as f64, oEY as u8 as f64, EZ as u8 as f64]
}

pub(super) fn canonical_temperature_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 1] {
	let A=(temperature+ parameters[5])- 273.15f64;
    [A]
}


static CANONICAL_MODEL_CACHE: OnceLock<Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>>> = OnceLock::new();

fn canonical_model_cache() -> &'static Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>> {
    CANONICAL_MODEL_CACHE.get_or_init(|| Mutex::new(HashMap::new()))
}

fn canonical_model_cache_lookup(key: &[u64]) -> Option<Arc<CanonicalModelValues>> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    let found = cache.get(key).and_then(Weak::upgrade);
    if found.is_none() {
        cache.remove(key);
    }
    found
}

fn canonical_model_cache_intern(
    key: Box<[u64]>,
    candidate: Arc<CanonicalModelValues>,
) -> Arc<CanonicalModelValues> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    if let Some(existing) = cache.get(key.as_ref()).and_then(Weak::upgrade) {
        return existing;
    }
    cache.retain(|_, values| values.strong_count() > 0);
    cache.insert(key, Arc::downgrade(&candidate));
    candidate
}

impl Instance {
    fn canonical_model_key(&self) -> Box<[u64]> {
        let mut key = Vec::with_capacity(84);
        for index in 0..Self::PARAMETER_COUNT {
            if PARAMETER_MODEL_FLAGS[index] {
                key.push(self.params.values[index].to_bits());
                key.push(u64::from(self.param_given[index]));
            }
        }
        key.into_boxed_slice()
    }

    fn canonical_install_model_values(&mut self, values: Arc<CanonicalModelValues>) {
        install_generated_stage_values(&mut self.canonical_staged[..], values.as_ref(), &CANONICAL_MODEL_STAGE_SLOTS);
        self.canonical_model_values = Some(values);
    }

    fn canonical_model_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_model_values.is_some() {
            return;
        }
        let key = self.canonical_model_key();
        if let Some(values) = canonical_model_cache_lookup(key.as_ref()) {
            self.canonical_install_model_values(values);
            return;
        }
        let produced: CanonicalModelValues = canonical_model_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        let values = canonical_model_cache_intern(key, Arc::new(produced));
        self.canonical_install_model_values(values);
    }

    fn canonical_instance_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_instance_valid {
            return;
        }
        let produced = canonical_instance_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_INSTANCE_STAGE_SLOTS);
        self.canonical_instance_valid = true;
    }

    fn canonical_temperature_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        let temperature = ctx.temperature();
        let thermal_voltage = ctx.thermal_voltage();
        if self.canonical_temperature_valid
            && self.canonical_temperature == temperature
            && self.canonical_thermal_voltage == thermal_voltage
        {
            return;
        }
        let produced = canonical_temperature_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_TEMPERATURE_STAGE_SLOTS);
        self.canonical_temperature = temperature;
        self.canonical_thermal_voltage = thermal_voltage;
        self.canonical_temperature_valid = true;
    }

    fn canonical_timestep_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        let produced: [f64; 1] = {
            let multiplicity = self.multiplicity;
            let staged = &*self.canonical_staged;
            [0.0]
        };
    }

    pub fn stamp(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedStamper<'_>) {
        self.canonical_model_stage(ctx);
        self.canonical_instance_stage(ctx);
        self.canonical_temperature_stage(ctx);
        self.canonical_timestep_stage(ctx);
        let parameters = &self.params.values;
        let multiplicity = self.multiplicity;
        let staged = &*self.canonical_staged;
        let node_potentials = [ctx.node_voltage(self.nodes[0]), ctx.node_voltage(self.nodes[1]), ctx.node_voltage(self.nodes[2])];
        let ddt_scale_value = if ctx.dynamic_operators_enabled() { self.ddt_coefficients.derivative_scale } else { 0.0 };
        let ddt_scale = move || ddt_scale_value;
        let ddt_state = self.stamp_state.as_mut();
        let dynamic_operators_enabled = ctx.dynamic_operators_enabled();
        let ddt_coefficients = self.ddt_coefficients;
        let mut ddt = |slot: usize, value: f64| -> f64 {
            if dynamic_operators_enabled {
                match rspice_eval_ddt(
                    &mut ddt_state.ddt_current,
                    &ddt_state.ddt_previous,
                    &ddt_state.ddt_older,
                    &ddt_state.ddt_initialized,
                    &mut ddt_state.ddt_derivative_current,
                    &ddt_state.ddt_derivative_previous,
                    &mut ddt_state.ddt_candidate_valid,
                    ddt_coefficients,
                    slot,
                    value,
                ) {
                    Ok(result) => result,
                    Err(source) => {
                        ctx.report_ddt_candidate_error(slot, source);
                        0.0
                    }
                }
            } else {
                0.0
            }
        };
		let A=parameters[7];
		let B=node_potentials[2];
		let F=parameters[35];
		let G=1f64;
		let K=parameters[36];
		let P=staged[4];
		let AE=staged[6];
		let AJ=staged[30]!=0.0;
		let AK=staged[9];
		let AN=parameters[28];
		let AR=parameters[26];
		let AX=0.3333333333333333f64;
		let AY=parameters[29];
		let AZ=parameters[27];
		let BC=L2([0f64;2]);
		let BN=staged[11];
		let BQ=staged[12];
		let BT=0f64;
		let BU=0f64;
		let BX=L3([0f64;3]);
		let CF=ddt_scale();
		let CV=0.0;
		let CW=0.0;
		let C=1f64* A;
		let D=staged[1]+ (A* B);
		let E=D< staged[2];
		let M;
		let N;
		if E{
		let H=((D- F)- G).exp();
		let I=C* H;
		let J=F+ H;
		M=J;
		N=I;
		}else{
		let L=D> (K- G);
		let X;
		let Y;
		if L{
		let U=((K- D)- G).exp();
		let V=K- U;
		let W=((C* -1f64)* U)* -1f64;
		X=V;
		Y=W;
		}else{
		X=D;
		Y=C;
		}
		M=X;
		N=Y;
		}
		let O=(M+ 273.15f64)- staged[3];
		let Q=staged[5]+ (O* P);
		let R=(N* Q)+ ((N* P)* O);
		let S=G+ (O* Q);
		let T=S< 0.11f64;
		let AC;
		let AD;
		if T{
		let Z=((10f64* (S- 0.01f64))- G).exp();
		let AA=((R* 10f64)* Z)* 0.1f64;
		let AB=0.01f64+ (0.1f64* Z);
		AC=AB;
		AD=AA;
		}else{
		AC=S;
		AD=R;
		}
		let AF=AE* AC;
		let AG=AD* AE;
		let AH=node_potentials[0]- node_potentials[1];
		let AI=L2([1f64,0.0])- L2([0.0,1f64]);
		let BD;
		let BE;
		if AJ{
		let AL=AH/ AK;
		let AM=AI/ AK;
		let AO=AN* AL;
		let AP=(AM* AN)* AO;
		let AQ=(G+ (AO* AO)).sqrt();
		let AS=AR* (AL.abs());
		let AT=(AM* ((2f64* ((AL>= 0f64) as u8 as f64))- 1f64))* AR;
		let AU=AS* AS;
		let AV=AT* AS;
		let AW=G+ (AU* AS);
		let BA=(staged[10]+ (AY* AQ))+ (AZ* (AW.powf(AX)));
		let BB=(((AP+ AP)* (1f64/ (2f64* AQ)))* AY)+ (((((AV+ AV)* AS)+ (AT* AU))* (AX* (AW.powf(-0.6666666666666667f64))))* AZ);
		BD=BA;
		BE=BB;
		}else{
		BD=G;
		BE=BC;
		}
		let BF=AF* BD;
		let BG=BE* AF;
		let BH=AH/ BF;
		let BI=(L3([AI[0],AI[1],0.0])- ((L3([0.0,0.0,(AG* BD)])+ L3([BG[0],BG[1],0.0]))* BH))/ BF;
		let BJ=-AH;
		let BK=BJ* BH;
		let BL=(AI* -1f64)* BH;
		let BM=L3([BL[0],BL[1],0.0])+ (BI* BJ);
		let BO=B* BN;
		let BP=1f64* BN;
		let BR=B* BQ;
		let BS=1f64* BQ;
		let BY;
		let BZ;
		let CA;
		let CB;
		let CC;
		let CD;
		if A!=0.0{
		BY=BO;
		BZ=BK;
		CA=BT;
		CB=BP;
		CC=BM;
		CD=BU;
		}else{
		let BV=1e6f64* B;
		let BW=1f64* 1e6f64;
		BY=BT;
		BZ=BT;
		CA=BV;
		CB=BU;
		CC=BX;
		CD=BW;
		}
		let CH;
		let CI;
		let CJ;
		let CK;
		if A!=0.0{
		let CE=ddt(0, BR);
		let CG=BS* CF;
		CH=CE;
		CI=BR;
		CJ=CG;
		CK=BS;
		}else{
		CH=BT;
		CI=BT;
		CJ=BU;
		CK=BU;
		}
		let CL=BI[0];
		let CM=BI[1];
		let CN=BI[2];
		let CO=CB;
		let CP=CC[0];
		let CQ=CC[1];
		let CR=CC[2];
		let CS=CD;
		let CT=CJ;
		let CU=CK;
        stamper.stamp_current_sparse_local::<3, 0>(
            Some(0),
            Some(1),
            multiplicity * (BH),
            [0, 1, 2],
            [CL, CM, CN],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (BY),
            [2],
            [CO],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<3, 0>(
            Some(2),
            None,
            multiplicity * (BZ),
            [0, 1, 2],
            [CP, CQ, CR],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (CA),
            [2],
            [CS],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (CH),
            [2],
            [CT],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (CV),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (CW),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        self.canonical_reactive[0] = BH;
        self.canonical_reactive[1] = BY;
        self.canonical_reactive[2] = BZ;
        self.canonical_reactive[3] = CA;
        self.canonical_reactive[4] = CI;
        self.canonical_reactive[5] = CU;
        self.canonical_reactive[6] = CV;
        self.canonical_reactive[7] = CW;
    }

    pub fn stamp_reactive(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedReactiveStamper<'_>) {
        let multiplicity = self.multiplicity;
        let cached = &*self.canonical_reactive;
        stamper.stamp_current_reactive_indexed_dense_local(
            Some(2),
            None,
            &[2],
            &[cached[5]],
            &[],
            &[],
            multiplicity,
        );
    }

}
