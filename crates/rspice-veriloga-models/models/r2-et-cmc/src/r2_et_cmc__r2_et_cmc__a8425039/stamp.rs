// @generated by rspice-veriloga; do not edit.
#![allow(dead_code, non_snake_case, unused_imports, unused_mut, unused_parens, unused_variables)]

use super::state::{CanonicalModelValues, Instance, PARAMETER_MODEL_FLAGS};
use rspice_veriloga_runtime::{GeneratedEvalContext, GeneratedReactiveStamper, GeneratedStamper, install_generated_stage_values, L2, L3, evaluate_generated_above, evaluate_generated_cross, evaluate_generated_timer, rspice_eval_ddt, rspice_eval_idt, rspice_limexp, rspice_limited_exp, rspice_limited_exp_derivative};
use std::collections::HashMap;
use std::sync::{Arc, Mutex, OnceLock, Weak};
pub(super) const CANONICAL_MODEL_STAGE_SLOTS: [u32; 5] = [5, 6, 4, 1, 3];
pub(super) const CANONICAL_INSTANCE_STAGE_SLOTS: [u32; 11] = [7, 8, 9, 10, 0, 11, 12, 14, 13, 15, 16];
pub(super) const CANONICAL_TEMPERATURE_STAGE_SLOTS: [u32; 1] = [2];

pub(super) fn canonical_model_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 5] {
	let B=parameter_given[11] as u8 as f64;
	let mut oC=0.0;
	let A=parameters[15]!= 1002f64;
	if parameter_given[11]{
	let C=1f64- (0.01f64* parameters[11]);
	oC=C;
	}
	let D=273.15f64+ parameters[16];
	let E=(parameters[29]> 0f64)|| (parameters[27]> 0f64);
	let F=parameters[35]+ 1f64;
    [A as u8 as f64, oC, D, E as u8 as f64, F]
}

pub(super) fn canonical_instance_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 11] {
	let A=parameters[3];
	let B=parameters[4];
	let D=parameters[23];
	let G=parameter_given[1] as u8 as f64;
	let H=parameter_given[2] as u8 as f64;
	let K=0f64;
	let M=parameters[2];
	let N=parameters[1];
	let R=parameters[0];
	let mut oE=false;
	let mut oO=false;
	let mut oP=false;
	let mut oQ=false;
	let mut oS=false;
	let mut oT=false;
	let mut oU=false;
	let mut oV=false;
	let C=A!=0.0&& B!=0.0;
	let F;
	if C{
	F=D;
	}else{
	let E=A!=0.0|| B!=0.0;
	oE=E;
	let L=if E{
	let J=D* 0.5f64;
	J
	}else{
	K
	};
	F=L;
	}
	let I=(parameter_given[1]&& parameter_given[2])&& (!(parameter_given[0]));
	if I{
	let O=(M== K)|| (N== K);
	oO=O;
	}else{
	let P=parameter_given[2]&& (!(parameter_given[1]));
	oP=P;
	if P{
	let Q=M== K;
	oQ=Q;
	if !Q{
	let T=R== K;
	oT=T;
	}
	}else{
	let S=R== K;
	oS=S;
	if !S{
	let U=N== K;
	oU=U;
	}
	}
	}
	if !C{
	let V=A!=0.0|| B!=0.0;
	oV=V;
	}
    [C as u8 as f64, oE as u8 as f64, I as u8 as f64, oO as u8 as f64, F, oP as u8 as f64, oQ as u8 as f64, oT as u8 as f64, oS as u8 as f64, oU as u8 as f64, oV as u8 as f64]
}

pub(super) fn canonical_temperature_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 1] {
	let A=(temperature+ parameters[5])- 273.15f64;
    [A]
}


static CANONICAL_MODEL_CACHE: OnceLock<Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>>> = OnceLock::new();

fn canonical_model_cache() -> &'static Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>> {
    CANONICAL_MODEL_CACHE.get_or_init(|| Mutex::new(HashMap::new()))
}

fn canonical_model_cache_lookup(key: &[u64]) -> Option<Arc<CanonicalModelValues>> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    let found = cache.get(key).and_then(Weak::upgrade);
    if found.is_none() {
        cache.remove(key);
    }
    found
}

fn canonical_model_cache_intern(
    key: Box<[u64]>,
    candidate: Arc<CanonicalModelValues>,
) -> Arc<CanonicalModelValues> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    if let Some(existing) = cache.get(key.as_ref()).and_then(Weak::upgrade) {
        return existing;
    }
    cache.retain(|_, values| values.strong_count() > 0);
    cache.insert(key, Arc::downgrade(&candidate));
    candidate
}

impl Instance {
    fn canonical_model_key(&self) -> Box<[u64]> {
        let mut key = Vec::with_capacity(84);
        for index in 0..Self::PARAMETER_COUNT {
            if PARAMETER_MODEL_FLAGS[index] {
                key.push(self.params.values[index].to_bits());
                key.push(u64::from(self.param_given[index]));
            }
        }
        key.into_boxed_slice()
    }

    fn canonical_install_model_values(&mut self, values: Arc<CanonicalModelValues>) {
        install_generated_stage_values(&mut self.canonical_staged[..], values.as_ref(), &CANONICAL_MODEL_STAGE_SLOTS);
        self.canonical_model_values = Some(values);
    }

    fn canonical_model_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_model_values.is_some() {
            return;
        }
        let key = self.canonical_model_key();
        if let Some(values) = canonical_model_cache_lookup(key.as_ref()) {
            self.canonical_install_model_values(values);
            return;
        }
        let produced: CanonicalModelValues = canonical_model_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        let values = canonical_model_cache_intern(key, Arc::new(produced));
        self.canonical_install_model_values(values);
    }

    fn canonical_instance_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_instance_valid {
            return;
        }
        let produced = canonical_instance_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_INSTANCE_STAGE_SLOTS);
        self.canonical_instance_valid = true;
    }

    fn canonical_temperature_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        let temperature = ctx.temperature();
        let thermal_voltage = ctx.thermal_voltage();
        if self.canonical_temperature_valid
            && self.canonical_temperature == temperature
            && self.canonical_thermal_voltage == thermal_voltage
        {
            return;
        }
        let produced = canonical_temperature_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_TEMPERATURE_STAGE_SLOTS);
        self.canonical_temperature = temperature;
        self.canonical_thermal_voltage = thermal_voltage;
        self.canonical_temperature_valid = true;
    }

    fn canonical_timestep_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        let produced: [f64; 1] = {
            let multiplicity = self.multiplicity;
            let staged = &*self.canonical_staged;
            [0.0]
        };
    }

    pub fn stamp(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedStamper<'_>) {
        if ctx.analog_tasks_enabled() { self.analog_effects.get_or_insert_with(Default::default).begin_evaluation(); }
        self.canonical_model_stage(ctx);
        self.canonical_instance_stage(ctx);
        self.canonical_temperature_stage(ctx);
        self.canonical_timestep_stage(ctx);
        let parameters = &self.params.values;
        let parameter_given = &*self.param_given;
        let multiplicity = self.multiplicity;
        let time = self.time;
        let analog_effects = &mut self.analog_effects;
        let mut analog_finish = |site: u32, time: f64, diagnostic: f64| {
            if ctx.analog_tasks_enabled() {
                if let Err(source) = analog_effects.get_or_insert_with(Default::default).record_finish(site, time, diagnostic) {
                    ctx.report_analog_task_error(site, source);
                }
            }
        };
        let staged = &*self.canonical_staged;
        let node_potentials = [ctx.node_voltage(self.nodes[0]), ctx.node_voltage(self.nodes[1]), ctx.node_voltage(self.nodes[2])];
        let ddt_scale_value = if ctx.dynamic_operators_enabled() { self.ddt_coefficients.derivative_scale } else { 0.0 };
        let ddt_scale = move || ddt_scale_value;
        let ddt_state = self.stamp_state.as_mut();
        let dynamic_operators_enabled = ctx.dynamic_operators_enabled();
        let ddt_coefficients = self.ddt_coefficients;
        let mut ddt = |slot: usize, value: f64| -> f64 {
            if dynamic_operators_enabled {
                match rspice_eval_ddt(
                    &mut ddt_state.ddt_current,
                    &ddt_state.ddt_previous,
                    &ddt_state.ddt_older,
                    &ddt_state.ddt_initialized,
                    &mut ddt_state.ddt_derivative_current,
                    &ddt_state.ddt_derivative_previous,
                    &mut ddt_state.ddt_candidate_valid,
                    ddt_coefficients,
                    slot,
                    value,
                ) {
                    Ok(result) => result,
                    Err(source) => {
                        ctx.report_ddt_candidate_error(slot, source);
                        0.0
                    }
                }
            } else {
                0.0
            }
        };
		let A=staged[5]!=0.0;
		let B=1f64;
		let D=parameter_given[10] as u8 as f64;
		let E=parameters[10];
		let H=parameter_given[11] as u8 as f64;
		let I=staged[6];
		let J=0f64;
		let K=0.01f64;
		let N=1e6f64;
		let P=staged[7]!=0.0;
		let Q=staged[9]!=0.0;
		let R=staged[10]!=0.0;
		let S=staged[11]!=0.0;
		let Y=parameters[0];
		let AA=parameters[22];
		let AC=1e99f64;
		let AD=parameters[1];
		let AF=staged[0];
		let AP=parameters[17];
		let AQ=parameters[2];
		let BA=staged[12]!=0.0;
		let BB=staged[13]!=0.0;
		let BJ=staged[14]!=0.0;
		let CM=staged[15]!=0.0;
		let DM=parameters[25];
		let DN=parameters[24];
		let DS=staged[1]!=0.0;
		let DW=parameters[37];
		let DX=parameters[38];
		let EB=parameters[39];
		let ED=parameters[40];
		let ER=staged[16]!=0.0;
		let EZ=parameters[7];
		let FA=node_potentials[2];
		let FE=parameters[35];
		let FI=parameters[36];
		let GI=parameters[28];
		let GM=parameters[26];
		let GS=0.3333333333333333f64;
		let GT=parameters[29];
		let GU=parameters[27];
		let GX=L2([0f64;2]);
		let HM=0f64;
		let HP=L3([0f64;3]);
		let HX=ddt_scale();
		let IN=0.0;
		let IO=0.0;
		if A{
		analog_finish(1, time, B);
		}
		let G=if parameter_given[10]{
		E
		}else{
		let F=ctx.simparam_or("scale", B);
		F
		};
		let M=if parameter_given[11]{
		I
		}else{
		let L=B- (K* (ctx.simparam_or("shrink", J)));
		L
		};
		let O=(M* G)* N;
		let T;
		let U;
		let V;
		let W;
		let X;
		if Q{
		let AI;
		let AJ;
		let AK;
		let AL;
		let AM;
		if R{
		let Z=Y* O;
		let AB=Z+ AA;
		AI=J;
		AJ=Z;
		AK=J;
		AL=J;
		AM=AB;
		}else{
		let AE=AD* O;
		let AG=AE+ AF;
		let AH=AG< J;
		if AH{
		analog_finish(32, time, B);
		}
		let AO=AG> J;
		let AW;
		let AX;
		let AY;
		if AO{
		let AR=(AP/ AQ)* AG;
		let AS=AR- AA;
		let AT=AS<= J;
		if AT{
		analog_finish(37, time, B);
		}
		AW=AS;
		AX=AQ;
		AY=AR;
		}else{
		let AU=Y* O;
		let AV=AU+ AA;
		AW=AU;
		AX=J;
		AY=AV;
		}
		AI=AE;
		AJ=AW;
		AK=AG;
		AL=AX;
		AM=AY;
		}
		T=AI;
		U=AJ;
		V=AK;
		W=AL;
		X=AM;
		}else{
		let BC;
		let BD;
		let BE;
		let BF;
		let BG;
		if S{
		let BK;
		let BL;
		let BM;
		let BN;
		let BO;
		if BA{
		let BH=Y* O;
		let BI=BH+ AA;
		BK=J;
		BL=BH;
		BM=J;
		BN=J;
		BO=BI;
		}else{
		let BU;
		let BV;
		let BW;
		let BX;
		let BY;
		if BJ{
		let BP=AD* O;
		let BQ=BP+ AF;
		BU=BP;
		BV=J;
		BW=BQ;
		BX=AC;
		BY=J;
		}else{
		let BR=Y* O;
		let BS=BR+ AA;
		let BT=BS< J;
		if BT{
		analog_finish(62, time, B);
		}
		let CA=BS> J;
		let CG;
		let CH;
		let CI;
		if CA{
		let CB=(AQ/ AP)* BS;
		let CC=CB- AF;
		let CD=CC<= J;
		if CD{
		analog_finish(67, time, B);
		}
		CG=CC;
		CH=CB;
		CI=AQ;
		}else{
		let CE=AD* O;
		let CF=CE+ AF;
		CG=CE;
		CH=CF;
		CI=AC;
		}
		BU=CG;
		BV=BR;
		BW=CH;
		BX=CI;
		BY=BS;
		}
		BK=BU;
		BL=BV;
		BM=BW;
		BN=BX;
		BO=BY;
		}
		BC=BK;
		BD=BL;
		BE=BM;
		BF=BN;
		BG=BO;
		}else{
		let CN;
		let CO;
		let CP;
		let CQ;
		let CR;
		if BB{
		let CK=AD* O;
		let CL=CK+ AF;
		CN=CK;
		CO=J;
		CP=CL;
		CQ=AC;
		CR=J;
		}else{
		let CX;
		let CY;
		let CZ;
		let DA;
		let DB;
		if CM{
		let CS=Y* O;
		let CT=CS+ AA;
		CX=J;
		CY=CS;
		CZ=J;
		DA=J;
		DB=CT;
		}else{
		let CU=Y* O;
		let CV=CU+ AA;
		let CW=CV< J;
		if CW{
		analog_finish(91, time, B);
		}
		let DD=AD* O;
		let DE=DD+ AF;
		let DF=CV> J;
		let DH;
		if DF{
		let DG=DE< J;
		if DG{
		analog_finish(96, time, B);
		}
		let DJ=DE> J;
		let DL;
		if DJ{
		let DK=AP* (DE/ CV);
		DL=DK;
		}else{
		DL=J;
		}
		DH=DL;
		}else{
		DH=AC;
		}
		CX=DD;
		CY=CU;
		CZ=DE;
		DA=DH;
		DB=CV;
		}
		CN=CX;
		CO=CY;
		CP=CZ;
		CQ=DA;
		CR=DB;
		}
		BC=CN;
		BD=CO;
		BE=CP;
		BF=CQ;
		BG=CR;
		}
		T=BC;
		U=BD;
		V=BE;
		W=BF;
		X=BG;
		}
		let DQ=if DM!=0.0{
		let DO=V+ DN;
		DO
		}else{
		let DP=T+ DN;
		DP
		};
		let DR=W> J;
		let DT=((DQ<= J)&& DR)&& DS;
		if DT{
		analog_finish(112, time, B);
		}
		let DV=V> J;
		let DY;
		let DZ;
		if DV{
		let EG;
		let EH;
		if P{
		let EC=DW+ (EB/ V);
		let EE=DX+ (ED/ V);
		EG=EC;
		EH=EE;
		}else{
		let EF=parameters[3]!=0.0|| parameters[4]!=0.0;
		let EK;
		let EL;
		if EF{
		let EI=DW+ ((0.5f64* EB)/ V);
		let EJ=DX+ ((0.5f64* ED)/ V);
		EK=EI;
		EL=EJ;
		}else{
		EK=DW;
		EL=DX;
		}
		EG=EK;
		EH=EL;
		}
		DY=EG;
		DZ=EH;
		}else{
		DY=DW;
		DZ=DX;
		}
		let EA=X> J;
		let EO;
		let EP;
		if EA{
		let EM=DY+ (parameters[41]/ X);
		let EN=DZ+ (parameters[42]/ X);
		EO=EM;
		EP=EN;
		}else{
		EO=DY;
		EP=DZ;
		}
		let ES;
		if P{
		let EQ=2f64* (T+ U);
		ES=EQ;
		}else{
		let EY=if ER{
		let EW=(2f64* T)+ U;
		EW
		}else{
		let EX=2f64* T;
		EX
		};
		ES=EY;
		}
		let ET=T* U;
		let EU=(parameters[44]+ (parameters[45]* ES))+ (parameters[46]* ET);
		let EV=(parameters[47]+ (parameters[48]* ES))+ (parameters[49]* ET);
		let FB=1f64* EZ;
		let FC=staged[2]+ (EZ* FA);
		let FD=FC< staged[3];
		let FK;
		let FL;
		if FD{
		let FF=((FC- FE)- B).exp();
		let FG=FB* FF;
		let FH=FE+ FF;
		FK=FH;
		FL=FG;
		}else{
		let FJ=FC> (FI- B);
		let FU;
		let FV;
		if FJ{
		let FR=((FI- FC)- B).exp();
		let FS=FI- FR;
		let FT=((FB* -1f64)* FR)* -1f64;
		FU=FS;
		FV=FT;
		}else{
		FU=FC;
		FV=FB;
		}
		FK=FU;
		FL=FV;
		}
		let FM=(FK+ 273.15f64)- staged[4];
		let FN=EO+ (FM* EP);
		let FO=(FL* FN)+ ((FL* EP)* FM);
		let FP=B+ (FM* FN);
		let FQ=FP< 0.11f64;
		let FZ;
		let GA;
		if FQ{
		let FW=((10f64* (FP- K))- B).exp();
		let FX=((FO* 10f64)* FW)* 0.1f64;
		let FY=K+ (0.1f64* FW);
		FZ=FY;
		GA=FX;
		}else{
		FZ=FP;
		GA=FO;
		}
		let GB=W* FZ;
		let GC=GA* W;
		let GD=node_potentials[0]- node_potentials[1];
		let GE=L2([1f64,0.0])- L2([0.0,1f64]);
		let GF=DR&& DS;
		let GY;
		let GZ;
		if GF{
		let GG=GD/ DQ;
		let GH=GE/ DQ;
		let GJ=GI* GG;
		let GK=(GH* GI)* GJ;
		let GL=(B+ (GJ* GJ)).sqrt();
		let GN=GM* (GG.abs());
		let GO=(GH* ((2f64* ((GG>= 0f64) as u8 as f64))- 1f64))* GM;
		let GP=GN* GN;
		let GQ=GO* GN;
		let GR=B+ (GP* GN);
		let GV=(((B- GT)- GU)+ (GT* GL))+ (GU* (GR.powf(GS)));
		let GW=(((GK+ GK)* (1f64/ (2f64* GL)))* GT)+ (((((GQ+ GQ)* GN)+ (GO* GP))* (GS* ((GR+ (((GR== 0f64) as u8 as f64)* 2.2250738585072014e-308f64)).powf(-0.6666666666666667f64))))* GU);
		GY=GV;
		GZ=GW;
		}else{
		GY=B;
		GZ=GX;
		}
		let HA=GB* GY;
		let HB=GZ* GB;
		let HC=GD/ HA;
		let HD=(L3([GE[0],GE[1],0.0])- ((L3([0.0,0.0,(GC* GY)])+ L3([HB[0],HB[1],0.0]))* HC))/ HA;
		let HE=-GD;
		let HF=HE* HC;
		let HG=(GE* -1f64)* HC;
		let HH=L3([HG[0],HG[1],0.0])+ (HD* HE);
		let HI=FA* EU;
		let HJ=1f64* EU;
		let HK=FA* EV;
		let HL=1f64* EV;
		let HQ;
		let HR;
		let HS;
		let HT;
		let HU;
		let HV;
		if EZ!=0.0{
		HQ=HI;
		HR=HF;
		HS=J;
		HT=HJ;
		HU=HH;
		HV=HM;
		}else{
		let HN=N* FA;
		let HO=1f64* N;
		HQ=J;
		HR=J;
		HS=HN;
		HT=HM;
		HU=HP;
		HV=HO;
		}
		let HZ;
		let IA;
		let IB;
		let IC;
		if EZ!=0.0{
		let HW=ddt(0, HK);
		let HY=HL* HX;
		HZ=HW;
		IA=HK;
		IB=HY;
		IC=HL;
		}else{
		HZ=J;
		IA=J;
		IB=HM;
		IC=HM;
		}
		let ID=HD[0];
		let IE=HD[1];
		let IF=HD[2];
		let IG=HT;
		let IH=HU[0];
		let II=HU[1];
		let IJ=HU[2];
		let IK=HV;
		let IL=IB;
		let IM=IC;
        stamper.stamp_current_sparse_local::<3, 0>(
            Some(0),
            Some(1),
            multiplicity * (HC),
            [0, 1, 2],
            [ID, IE, IF],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (HQ),
            [2],
            [IG],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<3, 0>(
            Some(2),
            None,
            multiplicity * (HR),
            [0, 1, 2],
            [IH, II, IJ],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (HS),
            [2],
            [IK],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (HZ),
            [2],
            [IL],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (IN),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (IO),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        self.canonical_reactive[0] = HC;
        self.canonical_reactive[1] = HQ;
        self.canonical_reactive[2] = HR;
        self.canonical_reactive[3] = HS;
        self.canonical_reactive[4] = IA;
        self.canonical_reactive[5] = IM;
        self.canonical_reactive[6] = IN;
        self.canonical_reactive[7] = IO;
        if ctx.analog_tasks_enabled() && !ctx.evaluation_failed() { self.analog_effects.as_mut().expect("task evaluation began").complete_evaluation(); }
    }

    pub fn stamp_reactive(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedReactiveStamper<'_>) {
        let multiplicity = self.multiplicity;
        let cached = &*self.canonical_reactive;
        stamper.stamp_current_reactive_indexed_dense_local(
            Some(2),
            None,
            &[2],
            &[cached[5]],
            &[],
            &[],
            multiplicity,
        );
    }

}
