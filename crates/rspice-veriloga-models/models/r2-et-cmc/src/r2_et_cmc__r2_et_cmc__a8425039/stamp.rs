// @generated by rspice-veriloga; do not edit.
#![allow(dead_code, non_snake_case, unused_imports, unused_mut, unused_parens, unused_variables)]

use super::state::{CanonicalModelValues, Instance, PARAMETER_MODEL_FLAGS};
use rspice_veriloga_runtime::{GeneratedEvalContext, GeneratedReactiveStamper, GeneratedStamper, install_generated_stage_values, L2, L3, integer, arithmetic::product_div, arithmetic::product_sum_div, arithmetic::sum_products_div, arithmetic::sum_products_div_lanes, evaluate_generated_above, evaluate_generated_cross, evaluate_generated_timer, rspice_eval_ddt, rspice_eval_idt, rspice_limexp, rspice_limited_exp, rspice_limited_exp_derivative, GeneratedDdtCandidateError, evaluate_generated_ddt_derivative};
use std::collections::HashMap;
use std::sync::{Arc, Mutex, OnceLock, Weak};
pub(super) const CANONICAL_MODEL_STAGE_SLOTS: [u32; 7] = [5, 6, 4, 1, 3, 17, 18];
pub(super) const CANONICAL_INSTANCE_STAGE_SLOTS: [u32; 11] = [7, 8, 9, 10, 0, 11, 12, 14, 13, 15, 16];
pub(super) const CANONICAL_TEMPERATURE_STAGE_SLOTS: [u32; 1] = [2];

pub(super) fn canonical_model_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 7] {
	let B=parameter_given[11] as u8 as f64;
	let G=0f64;
	let I=0f64;
	let mut oC=0.0;
	let A=parameters[15]!= 1002f64;
	if parameter_given[11]{
	let C=1f64- (0.01f64* parameters[11]);
	oC=C;
	}
	let D=273.15f64+ parameters[16];
	let E=(parameters[29]> 0f64)|| (parameters[27]> 0f64);
	let F=parameters[35]+ 1f64;
	let H=0f64+ G;
	let J=0f64+ I;
    [A as u8 as f64, oC, D, E as u8 as f64, F, H, J]
}

pub(super) fn canonical_instance_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 11] {
	let A=parameters[3];
	let B=parameters[4];
	let D=parameters[23];
	let G=parameter_given[1] as u8 as f64;
	let H=parameter_given[2] as u8 as f64;
	let K=0f64;
	let M=parameters[2];
	let N=parameters[1];
	let R=parameters[0];
	let mut oE=false;
	let mut oO=false;
	let mut oP=false;
	let mut oQ=false;
	let mut oS=false;
	let mut oT=false;
	let mut oU=false;
	let mut oV=false;
	let C=A!=0.0&& B!=0.0;
	let F;
	if C{
	F=D;
	}else{
	let E=A!=0.0|| B!=0.0;
	oE=E;
	let L=if E{
	let J=D* 0.5f64;
	J
	}else{
	K
	};
	F=L;
	}
	let I=(parameter_given[1]&& parameter_given[2])&& (!(parameter_given[0]));
	if I{
	let O=(M== K)|| (N== K);
	oO=O;
	}else{
	let P=parameter_given[2]&& (!(parameter_given[1]));
	oP=P;
	if P{
	let Q=M== K;
	oQ=Q;
	if !Q{
	let T=R== K;
	oT=T;
	}
	}else{
	let S=R== K;
	oS=S;
	if !S{
	let U=N== K;
	oU=U;
	}
	}
	}
	if !C{
	let V=A!=0.0|| B!=0.0;
	oV=V;
	}
    [C as u8 as f64, oE as u8 as f64, I as u8 as f64, oO as u8 as f64, F, oP as u8 as f64, oQ as u8 as f64, oT as u8 as f64, oS as u8 as f64, oU as u8 as f64, oV as u8 as f64]
}

pub(super) fn canonical_temperature_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 1] {
	let A=(temperature+ parameters[5])- 273.15f64;
    [A]
}


static CANONICAL_MODEL_CACHE: OnceLock<Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>>> = OnceLock::new();

fn canonical_model_cache() -> &'static Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>> {
    CANONICAL_MODEL_CACHE.get_or_init(|| Mutex::new(HashMap::new()))
}

fn canonical_model_cache_lookup(key: &[u64]) -> Option<Arc<CanonicalModelValues>> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    let found = cache.get(key).and_then(Weak::upgrade);
    if found.is_none() {
        cache.remove(key);
    }
    found
}

fn canonical_model_cache_intern(
    key: Box<[u64]>,
    candidate: Arc<CanonicalModelValues>,
) -> Arc<CanonicalModelValues> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    if let Some(existing) = cache.get(key.as_ref()).and_then(Weak::upgrade) {
        return existing;
    }
    cache.retain(|_, values| values.strong_count() > 0);
    cache.insert(key, Arc::downgrade(&candidate));
    candidate
}

impl Instance {
    fn canonical_model_key(&self) -> Box<[u64]> {
        let mut key = Vec::with_capacity(84);
        for index in 0..Self::PARAMETER_COUNT {
            if PARAMETER_MODEL_FLAGS[index] {
                key.push(self.params.values[index].to_bits());
                key.push(u64::from(self.param_given[index]));
            }
        }
        key.into_boxed_slice()
    }

    fn canonical_install_model_values(&mut self, values: Arc<CanonicalModelValues>) {
        install_generated_stage_values(&mut self.canonical_staged[..], values.as_ref(), &CANONICAL_MODEL_STAGE_SLOTS);
        self.canonical_model_values = Some(values);
    }

    fn canonical_model_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_model_values.is_some() {
            return;
        }
        let key = self.canonical_model_key();
        if let Some(values) = canonical_model_cache_lookup(key.as_ref()) {
            self.canonical_install_model_values(values);
            return;
        }
        let produced: CanonicalModelValues = canonical_model_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        let values = canonical_model_cache_intern(key, Arc::new(produced));
        self.canonical_install_model_values(values);
    }

    fn canonical_instance_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_instance_valid {
            return;
        }
        let produced = canonical_instance_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_INSTANCE_STAGE_SLOTS);
        self.canonical_instance_valid = true;
    }

    fn canonical_temperature_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        let temperature = ctx.temperature();
        let thermal_voltage = ctx.thermal_voltage();
        if self.canonical_temperature_valid
            && self.canonical_temperature == temperature
            && self.canonical_thermal_voltage == thermal_voltage
        {
            return;
        }
        let produced = canonical_temperature_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_TEMPERATURE_STAGE_SLOTS);
        self.canonical_temperature = temperature;
        self.canonical_thermal_voltage = thermal_voltage;
        self.canonical_temperature_valid = true;
    }

    pub fn stamp(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedStamper<'_>) {
        if ctx.analog_tasks_enabled() { self.analog_effects.get_or_insert_with(Default::default).begin_evaluation(); }
        self.canonical_model_stage(ctx);
        self.canonical_instance_stage(ctx);
        self.canonical_temperature_stage(ctx);
        let parameters = &self.params.values;
        let parameter_given = &*self.param_given;
        let multiplicity = self.multiplicity;
        let time = self.time;
        let analog_effects = &mut self.analog_effects;
        let mut analog_finish = |site: u32, time: f64, diagnostic: f64| {
            if ctx.analog_tasks_enabled() {
                if let Err(source) = analog_effects.get_or_insert_with(Default::default).record_finish(site, time, diagnostic) {
                    ctx.report_analog_task_error(site, source);
                }
            }
        };
        let staged = &*self.canonical_staged;
        let node_potentials = [ctx.node_voltage(self.nodes[0]), ctx.node_voltage(self.nodes[1]), ctx.node_voltage(self.nodes[2])];
        let ddt_state = self.stamp_state.as_mut();
        let ddt_derivative_coefficients = self.ddt_coefficients;
        let ddt_derivative = |slot: usize, primal: f64, input: f64| -> f64 {
            let result = if !primal.is_finite() || !input.is_finite() {
                Err(GeneratedDdtCandidateError::NonFiniteInput { field: "derivative operands" })
            } else if !ctx.integration_operators_enabled() { Ok(0.0) } else {
                evaluate_generated_ddt_derivative(ddt_derivative_coefficients, ddt_state.ddt_initialized[slot], input)
            };
            match result {
                Ok(value) => value,
                Err(source) => { ctx.report_ddt_candidate_error(slot, source); 0.0 }
            }
        };
        let integration_operators_enabled = ctx.integration_operators_enabled();
        let ddt_coefficients = self.ddt_coefficients;
        let mut ddt = |slot: usize, value: f64| -> f64 {
            if integration_operators_enabled {
                match rspice_eval_ddt(
                    &mut ddt_state.ddt_current,
                    &ddt_state.ddt_previous,
                    &ddt_state.ddt_older,
                    &ddt_state.ddt_initialized,
                    &mut ddt_state.ddt_derivative_current,
                    &ddt_state.ddt_derivative_previous,
                    &mut ddt_state.ddt_candidate_valid,
                    ddt_coefficients,
                    slot,
                    value,
                ) {
                    Ok(result) => result,
                    Err(source) => {
                        ctx.report_ddt_candidate_error(slot, source);
                        0.0
                    }
                }
            } else {
                if !value.is_finite() { ctx.report_ddt_candidate_error(slot, GeneratedDdtCandidateError::NonFiniteInput { field: "input" }); }
                0.0
            }
        };
		let A=staged[5]!=0.0;
		let B=1f64;
		let D=parameter_given[10] as u8 as f64;
		let E=parameters[10];
		let H=parameter_given[11] as u8 as f64;
		let K=staged[6];
		let O=0f64;
		let Q=0.01f64;
		let S=1e6f64;
		let U=staged[7]!=0.0;
		let V=staged[9]!=0.0;
		let W=staged[10]!=0.0;
		let X=staged[11]!=0.0;
		let AD=parameters[0];
		let AF=parameters[22];
		let AH=1e99f64;
		let AI=parameters[1];
		let AK=staged[0];
		let AU=parameters[17];
		let AV=parameters[2];
		let BF=staged[12]!=0.0;
		let BG=staged[13]!=0.0;
		let BO=staged[14]!=0.0;
		let CR=staged[15]!=0.0;
		let DR=parameters[25];
		let DS=parameters[24];
		let DX=staged[1]!=0.0;
		let EB=parameters[37];
		let EC=parameters[38];
		let EG=parameters[39];
		let EI=parameters[40];
		let EW=staged[16]!=0.0;
		let FE=parameters[7];
		let FF=node_potentials[2];
		let FJ=parameters[35];
		let FN=parameters[36];
		let GN=parameters[28];
		let GR=parameters[26];
		let GX=0.3333333333333333f64;
		let GY=parameters[29];
		let GZ=parameters[27];
		let HC=L2([0f64;2]);
		let HU=0f64;
		let HX=L3([0f64;3]);
		if A{
		analog_finish(2, time, B);
		}
		let G;
		if parameter_given[10]{
		G=E;
		}else{
		let F=ctx.has_simparam("scale");
		let J=if F{
		let I=ctx.simparam_required("scale");
		I
		}else{
		B
		};
		G=J;
		}
		let M;
		if parameter_given[11]{
		M=K;
		}else{
		let L=ctx.has_simparam("shrink");
		let P=if L{
		let N=ctx.simparam_required("shrink");
		N
		}else{
		O
		};
		let R=B- (Q* P);
		M=R;
		}
		let T=(M* G)* S;
		let Y;
		let Z;
		let AA;
		let AB;
		let AC;
		if V{
		let AN;
		let AO;
		let AP;
		let AQ;
		let AR;
		if W{
		let AE=AD* T;
		let AG=AE+ AF;
		AN=O;
		AO=AE;
		AP=O;
		AQ=O;
		AR=AG;
		}else{
		let AJ=AI* T;
		let AL=AJ+ AK;
		let AM=AL< O;
		if AM{
		analog_finish(33, time, B);
		}
		let AT=AL> O;
		let BB;
		let BC;
		let BD;
		if AT{
		let AW=(AU/ AV)* AL;
		let AX=AW- AF;
		let AY=AX<= O;
		if AY{
		analog_finish(38, time, B);
		}
		BB=AX;
		BC=AV;
		BD=AW;
		}else{
		let AZ=AD* T;
		let BA=AZ+ AF;
		BB=AZ;
		BC=O;
		BD=BA;
		}
		AN=AJ;
		AO=BB;
		AP=AL;
		AQ=BC;
		AR=BD;
		}
		Y=AN;
		Z=AO;
		AA=AP;
		AB=AQ;
		AC=AR;
		}else{
		let BH;
		let BI;
		let BJ;
		let BK;
		let BL;
		if X{
		let BP;
		let BQ;
		let BR;
		let BS;
		let BT;
		if BF{
		let BM=AD* T;
		let BN=BM+ AF;
		BP=O;
		BQ=BM;
		BR=O;
		BS=O;
		BT=BN;
		}else{
		let BZ;
		let CA;
		let CB;
		let CC;
		let CD;
		if BO{
		let BU=AI* T;
		let BV=BU+ AK;
		BZ=BU;
		CA=O;
		CB=BV;
		CC=AH;
		CD=O;
		}else{
		let BW=AD* T;
		let BX=BW+ AF;
		let BY=BX< O;
		if BY{
		analog_finish(63, time, B);
		}
		let CF=BX> O;
		let CL;
		let CM;
		let CN;
		if CF{
		let CG=(AV/ AU)* BX;
		let CH=CG- AK;
		let CI=CH<= O;
		if CI{
		analog_finish(68, time, B);
		}
		CL=CH;
		CM=CG;
		CN=AV;
		}else{
		let CJ=AI* T;
		let CK=CJ+ AK;
		CL=CJ;
		CM=CK;
		CN=AH;
		}
		BZ=CL;
		CA=BW;
		CB=CM;
		CC=CN;
		CD=BX;
		}
		BP=BZ;
		BQ=CA;
		BR=CB;
		BS=CC;
		BT=CD;
		}
		BH=BP;
		BI=BQ;
		BJ=BR;
		BK=BS;
		BL=BT;
		}else{
		let CS;
		let CT;
		let CU;
		let CV;
		let CW;
		if BG{
		let CP=AI* T;
		let CQ=CP+ AK;
		CS=CP;
		CT=O;
		CU=CQ;
		CV=AH;
		CW=O;
		}else{
		let DC;
		let DD;
		let DE;
		let DF;
		let DG;
		if CR{
		let CX=AD* T;
		let CY=CX+ AF;
		DC=O;
		DD=CX;
		DE=O;
		DF=O;
		DG=CY;
		}else{
		let CZ=AD* T;
		let DA=CZ+ AF;
		let DB=DA< O;
		if DB{
		analog_finish(92, time, B);
		}
		let DI=AI* T;
		let DJ=DI+ AK;
		let DK=DA> O;
		let DM;
		if DK{
		let DL=DJ< O;
		if DL{
		analog_finish(97, time, B);
		}
		let DO=DJ> O;
		let DQ;
		if DO{
		let DP=AU* (DJ/ DA);
		DQ=DP;
		}else{
		DQ=O;
		}
		DM=DQ;
		}else{
		DM=AH;
		}
		DC=DI;
		DD=CZ;
		DE=DJ;
		DF=DM;
		DG=DA;
		}
		CS=DC;
		CT=DD;
		CU=DE;
		CV=DF;
		CW=DG;
		}
		BH=CS;
		BI=CT;
		BJ=CU;
		BK=CV;
		BL=CW;
		}
		Y=BH;
		Z=BI;
		AA=BJ;
		AB=BK;
		AC=BL;
		}
		let DV=if DR!=0.0{
		let DT=AA+ DS;
		DT
		}else{
		let DU=Y+ DS;
		DU
		};
		let DW=AB> O;
		let DY=((DV<= O)&& DW)&& DX;
		if DY{
		analog_finish(113, time, B);
		}
		let EA=AA> O;
		let ED;
		let EE;
		if EA{
		let EL;
		let EM;
		if U{
		let EH=EB+ (EG/ AA);
		let EJ=EC+ (EI/ AA);
		EL=EH;
		EM=EJ;
		}else{
		let EK=parameters[3]!=0.0|| parameters[4]!=0.0;
		let EP;
		let EQ;
		if EK{
		let EN=EB+ ((0.5f64* EG)/ AA);
		let EO=EC+ ((0.5f64* EI)/ AA);
		EP=EN;
		EQ=EO;
		}else{
		EP=EB;
		EQ=EC;
		}
		EL=EP;
		EM=EQ;
		}
		ED=EL;
		EE=EM;
		}else{
		ED=EB;
		EE=EC;
		}
		let EF=AC> O;
		let ET;
		let EU;
		if EF{
		let ER=ED+ (parameters[41]/ AC);
		let ES=EE+ (parameters[42]/ AC);
		ET=ER;
		EU=ES;
		}else{
		ET=ED;
		EU=EE;
		}
		let EX;
		if U{
		let EV=2f64* (Y+ Z);
		EX=EV;
		}else{
		let FD=if EW{
		let FB=(2f64* Y)+ Z;
		FB
		}else{
		let FC=2f64* Y;
		FC
		};
		EX=FD;
		}
		let EY=Y* Z;
		let EZ=(parameters[44]+ (parameters[45]* EX))+ (parameters[46]* EY);
		let FA=(parameters[47]+ (parameters[48]* EX))+ (parameters[49]* EY);
		let FG=1f64* FE;
		let FH=staged[2]+ (FE* FF);
		let FI=FH< staged[3];
		let FP;
		let FQ;
		if FI{
		let FK=((FH- FJ)- B).exp();
		let FL=FG* FK;
		let FM=FJ+ FK;
		FP=FM;
		FQ=FL;
		}else{
		let FO=FH> (FN- B);
		let FZ;
		let GA;
		if FO{
		let FW=((FN- FH)- B).exp();
		let FX=FN- FW;
		let FY=((FG* -1f64)* FW)* -1f64;
		FZ=FX;
		GA=FY;
		}else{
		FZ=FH;
		GA=FG;
		}
		FP=FZ;
		FQ=GA;
		}
		let FR=(FP+ 273.15f64)- staged[4];
		let FS=ET+ (FR* EU);
		let FT=(FQ* FS)+ ((FQ* EU)* FR);
		let FU=B+ (FR* FS);
		let FV=FU< 0.11f64;
		let GE;
		let GF;
		if FV{
		let GB=((10f64* (FU- Q))- B).exp();
		let GC=((FT* 10f64)* GB)* 0.1f64;
		let GD=Q+ (0.1f64* GB);
		GE=GD;
		GF=GC;
		}else{
		GE=FU;
		GF=FT;
		}
		let GG=AB* GE;
		let GH=GF* AB;
		let GI=node_potentials[0]- node_potentials[1];
		let GJ=L2([1f64,0.0])- L2([0.0,1f64]);
		let GK=DW&& DX;
		let HD;
		let HE;
		if GK{
		let GL=GI/ DV;
		let GM=GJ/ DV;
		let GO=GN* GL;
		let GP=(GM* GN)* GO;
		let GQ=(B+ (GO* GO)).sqrt();
		let GS=GR* (GL.abs());
		let GT=(GM* ((2f64* ((GL>= 0f64) as u8 as f64))- 1f64))* GR;
		let GU=GS* GS;
		let GV=GT* GS;
		let GW=B+ (GU* GS);
		let HA=(((B- GY)- GZ)+ (GY* GQ))+ (GZ* (GW.powf(GX)));
		let HB=(((GP+ GP)* (1f64/ (2f64* GQ)))* GY)+ (((((GV+ GV)* GS)+ (GT* GU))* (GX* ((GW+ (((GW== 0f64) as u8 as f64)* 2.2250738585072014e-308f64)).powf(-0.6666666666666667f64))))* GZ);
		HD=HA;
		HE=HB;
		}else{
		HD=B;
		HE=HC;
		}
		let HF=GG* HD;
		let HG=HE* GG;
		let HH=GI/ HF;
		let HI=(L3([GJ[0],GJ[1],0.0])).product_sum_div(1f64,(L3([0.0,0.0,(GH* HD)])+ L3([HG[0],HG[1],0.0])),(-HH),HF);
		let HJ=-GI;
		let HK=HJ* HH;
		let HL=(GJ* -1f64)* HH;
		let HM=L3([HL[0],HL[1],0.0])+ (HI* HJ);
		let HN=FF* EZ;
		let HO=1f64* EZ;
		let HP=FF* FA;
		let HQ=1f64* FA;
		let HR=O+ HH;
		let HY;
		let HZ;
		let IA;
		let IB;
		let IC;
		let ID;
		if FE!=0.0{
		let HS=O+ HN;
		let HT=O+ HK;
		HY=HS;
		HZ=HT;
		IA=O;
		IB=HO;
		IC=HM;
		ID=HU;
		}else{
		let HV=1f64* S;
		let HW=O+ (S* FF);
		HY=O;
		HZ=O;
		IA=HW;
		IB=HU;
		IC=HX;
		ID=HV;
		}
		let IH;
		let II;
		let IJ;
		let IK;
		if FE!=0.0{
		let IE=ddt(0, HP);
		let IF=ddt_derivative(0,IE,HQ);
		let IG=O+ IE;
		IH=IG;
		II=HP;
		IJ=IF;
		IK=HQ;
		}else{
		IH=O;
		II=O;
		IJ=HU;
		IK=HU;
		}
		let IL=HI[0];
		let IM=HI[1];
		let IN=HI[2];
		let IO=IB;
		let IP=IC[0];
		let IQ=IC[1];
		let IR=IC[2];
		let IS=ID;
		let IT=IJ;
		let IU=IK;
        stamper.stamp_current_sparse_local::<3, 0>(
            Some(0),
            Some(1),
            multiplicity * (HR),
            [0, 1, 2],
            [IL, IM, IN],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (HY),
            [2],
            [IO],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<3, 0>(
            Some(2),
            None,
            multiplicity * (HZ),
            [0, 1, 2],
            [IP, IQ, IR],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (IA),
            [2],
            [IS],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<1, 0>(
            Some(2),
            None,
            multiplicity * (IH),
            [2],
            [IT],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (staged[17]),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (staged[18]),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        self.canonical_reactive[0] = HR;
        self.canonical_reactive[1] = HY;
        self.canonical_reactive[2] = HZ;
        self.canonical_reactive[3] = IA;
        self.canonical_reactive[4] = II;
        self.canonical_reactive[5] = IU;
        self.canonical_reactive[6] = staged[17];
        self.canonical_reactive[7] = staged[18];
        if ctx.analog_tasks_enabled() && !ctx.evaluation_failed() { self.analog_effects.as_mut().expect("task evaluation began").complete_evaluation(); }
    }

    pub fn stamp_reactive(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedReactiveStamper<'_>) {
        let multiplicity = self.multiplicity;
        let cached = &*self.canonical_reactive;
        stamper.stamp_current_reactive_indexed_dense_local(
            Some(2),
            None,
            &[2],
            &[cached[5]],
            &[],
            &[],
            multiplicity,
        );
    }

}
