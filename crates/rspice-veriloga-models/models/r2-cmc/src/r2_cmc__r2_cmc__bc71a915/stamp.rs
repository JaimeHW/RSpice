// @generated by rspice-veriloga; do not edit.
#![allow(dead_code, non_snake_case, unused_imports, unused_mut, unused_parens, unused_variables)]

use super::state::{CanonicalModelValues, Instance, PARAMETER_MODEL_FLAGS};
use rspice_veriloga_runtime::{GeneratedEvalContext, GeneratedReactiveStamper, GeneratedStamper, install_generated_stage_values, L2, evaluate_generated_above, evaluate_generated_cross, evaluate_generated_timer, rspice_eval_ddt, rspice_eval_idt, rspice_limexp, rspice_limited_exp, rspice_limited_exp_derivative};
use std::collections::HashMap;
use std::sync::{Arc, Mutex, OnceLock, Weak};
pub(super) const CANONICAL_MODEL_STAGE_SLOTS: [u32; 4] = [2, 1, 0, 6];
pub(super) const CANONICAL_INSTANCE_STAGE_SLOTS: [u32; 25] = [12, 13, 14, 15, 17, 16, 18, 20, 21, 19, 22, 23, 24, 7, 5, 25, 27, 26, 4, 3, 29, 8, 30, 31, 32];
pub(super) const CANONICAL_TEMPERATURE_STAGE_SLOTS: [u32; 4] = [10, 11, 28, 9];

pub(super) fn canonical_model_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 4] {
	let A=parameter_given[9] as u8 as f64;
	let B=parameters[9];
	let C=1f64;
	let E=parameter_given[10] as u8 as f64;
	let G=1f64;
	let D=if parameter_given[9]{
	B
	}else{
	C
	};
	let H=if parameter_given[10]{
	let F=C- (0.01f64* parameters[10]);
	F
	}else{
	G
	};
	let I=(H* D)* 1e6f64;
	let J=273.15f64+ parameters[15];
	let K=parameters[34]+ C;
	let L=(parameters[28]> 0f64)|| (parameters[26]> 0f64);
    [I, J, K, L as u8 as f64]
}

pub(super) fn canonical_instance_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 25] {
	let A=parameters[3];
	let B=parameters[4];
	let D=parameters[22];
	let G=parameter_given[1] as u8 as f64;
	let H=parameter_given[2] as u8 as f64;
	let J=0.5f64;
	let L=0f64;
	let N=parameters[2];
	let O=parameters[1];
	let W=parameters[0];
	let X=staged[2];
	let Y=parameters[21];
	let AA=1e99f64;
	let AJ=parameters[16];
	let CV=parameters[24];
	let CW=parameters[23];
	let DC=parameters[36];
	let DD=parameters[37];
	let DH=parameters[38];
	let DJ=parameters[39];
	let mut oE=false;
	let mut oP=false;
	let mut oQ=false;
	let mut oAI=false;
	let mut oAQ=false;
	let mut oAR=false;
	let mut oAY=false;
	let mut oBM=false;
	let mut oBY=false;
	let mut oCN=false;
	let mut oCQ=false;
	let mut oDL=false;
	let mut oDX=0.0;
	let mut oEA=false;
	let C=A!=0.0&& B!=0.0;
	let F;
	if C{
	F=D;
	}else{
	let E=A!=0.0|| B!=0.0;
	oE=E;
	let M=if E{
	let K=D* J;
	K
	}else{
	L
	};
	F=M;
	}
	let I=(parameter_given[1]&& parameter_given[2])&& (!(parameter_given[0]));
	let R;
	let S;
	let T;
	let U;
	let V;
	if I{
	let P=(N== L)|| (O== L);
	oP=P;
	let AD;
	let AE;
	let AF;
	let AG;
	let AH;
	if P{
	let Z=(W* X)+ Y;
	AD=L;
	AE=L;
	AF=L;
	AG=Z;
	AH=AA;
	}else{
	let AB=O* X;
	let AC=AB+ F;
	let AI=AC> L;
	oAI=AI;
	let AM;
	let AN;
	let AO;
	if AI{
	let AK=(AJ/ N)* AC;
	let AP=1f64/ N;
	AM=N;
	AN=AK;
	AO=AP;
	}else{
	let AL=(W* X)+ Y;
	AM=L;
	AN=AL;
	AO=AA;
	}
	AD=AB;
	AE=AC;
	AF=AM;
	AG=AN;
	AH=AO;
	}
	R=AD;
	S=AE;
	T=AF;
	U=AG;
	V=AH;
	}else{
	let Q=parameter_given[2]&& (!(parameter_given[1]));
	oQ=Q;
	let AS;
	let AT;
	let AU;
	let AV;
	let AW;
	if Q{
	let AQ=N== L;
	oAQ=AQ;
	let AZ;
	let BA;
	let BB;
	let BC;
	let BD;
	if AQ{
	let AX=(W* X)+ Y;
	AZ=L;
	BA=L;
	BB=L;
	BC=AX;
	BD=AA;
	}else{
	let AY=W== L;
	oAY=AY;
	let BH;
	let BI;
	let BJ;
	let BK;
	let BL;
	if AY{
	let BE=O* X;
	let BF=BE+ F;
	BH=BE;
	BI=BF;
	BJ=AA;
	BK=L;
	BL=L;
	}else{
	let BG=(W* X)+ Y;
	let BM=BG> L;
	oBM=BM;
	let BR;
	let BS;
	let BT;
	let BU;
	if BM{
	let BN=(N/ AJ)* BG;
	let BO=BN- F;
	let BV=1f64/ N;
	BR=BO;
	BS=BN;
	BT=N;
	BU=BV;
	}else{
	let BP=O* X;
	let BQ=BP+ F;
	BR=BP;
	BS=BQ;
	BT=AA;
	BU=L;
	}
	BH=BR;
	BI=BS;
	BJ=BT;
	BK=BG;
	BL=BU;
	}
	AZ=BH;
	BA=BI;
	BB=BJ;
	BC=BK;
	BD=BL;
	}
	AS=AZ;
	AT=BA;
	AU=BB;
	AV=BC;
	AW=BD;
	}else{
	let AR=W== L;
	oAR=AR;
	let BZ;
	let CA;
	let CB;
	let CC;
	let CD;
	if AR{
	let BW=O* X;
	let BX=BW+ F;
	BZ=BW;
	CA=BX;
	CB=AA;
	CC=L;
	CD=L;
	}else{
	let BY=O== L;
	oBY=BY;
	let CG;
	let CH;
	let CI;
	let CJ;
	let CK;
	if BY{
	let CE=(W* X)+ Y;
	CG=L;
	CH=L;
	CI=L;
	CJ=CE;
	CK=AA;
	}else{
	let CF=(W* X)+ Y;
	let CL=O* X;
	let CM=CL+ F;
	let CN=CF> L;
	oCN=CN;
	let CO;
	let CP;
	if CN{
	let CQ=CM> L;
	oCQ=CQ;
	let CT;
	let CU;
	if CQ{
	let CR=AJ* (CM/ CF);
	let CS=1f64/ CR;
	CT=CR;
	CU=CS;
	}else{
	CT=L;
	CU=AA;
	}
	CO=CT;
	CP=CU;
	}else{
	CO=AA;
	CP=L;
	}
	CG=CL;
	CH=CM;
	CI=CO;
	CJ=CF;
	CK=CP;
	}
	BZ=CG;
	CA=CH;
	CB=CI;
	CC=CJ;
	CD=CK;
	}
	AS=BZ;
	AT=CA;
	AU=CB;
	AV=CC;
	AW=CD;
	}
	R=AS;
	S=AT;
	T=AU;
	U=AV;
	V=AW;
	}
	let CZ=if CV!=0.0{
	let CX=S+ CW;
	CX
	}else{
	let CY=R+ CW;
	CY
	};
	let DA=T> L;
	let DB=S> L;
	let DE;
	let DF;
	if DB{
	let DM;
	let DN;
	if C{
	let DI=DC+ (DH/ S);
	let DK=DD+ (DJ/ S);
	DM=DI;
	DN=DK;
	}else{
	let DL=A!=0.0|| B!=0.0;
	oDL=DL;
	let DQ;
	let DR;
	if DL{
	let DO=DC+ ((J* DH)/ S);
	let DP=DD+ ((J* DJ)/ S);
	DQ=DO;
	DR=DP;
	}else{
	DQ=DC;
	DR=DD;
	}
	DM=DQ;
	DN=DR;
	}
	DE=DM;
	DF=DN;
	}else{
	DE=DC;
	DF=DD;
	}
	let DG=U> L;
	let DU;
	let DV;
	if DG{
	let DS=DE+ (parameters[40]/ U);
	let DT=DF+ (parameters[41]/ U);
	DU=DS;
	DV=DT;
	}else{
	DU=DE;
	DV=DF;
	}
	let DW=DA&& (staged[6]!=0.0);
	if DW{
	let DX=(1f64- parameters[28])- parameters[26];
	oDX=DX;
	}
	let DY=V> L;
	let DZ=(parameters[6]!=0.0&& DA)&& DY;
	if DZ{
	let EA=(parameters[32]!=0.0&& DB)&& DG;
	oEA=EA;
	}
	let EB=DA&& DY;
    [C as u8 as f64, oE as u8 as f64, I as u8 as f64, oP as u8 as f64, oAI as u8 as f64, oQ as u8 as f64, oAQ as u8 as f64, oAY as u8 as f64, oBM as u8 as f64, oAR as u8 as f64, oBY as u8 as f64, oCN as u8 as f64, oCQ as u8 as f64, CZ, T, DB as u8 as f64, oDL as u8 as f64, DG as u8 as f64, DU, DV, DW as u8 as f64, oDX, DZ as u8 as f64, oEA as u8 as f64, EB as u8 as f64]
}

pub(super) fn canonical_temperature_preprocess(
    parameters: &[f64],
    parameter_given: &[bool],
    multiplicity: f64,
    staged: &[f64],
    temperature: f64,
    thermal_voltage: f64,
) -> [f64; 4] {
	let C=parameters[34];
	let D=1f64;
	let F=parameters[35];
	let mut oG=false;
	let A=(temperature+ parameters[5])- 273.15f64;
	let B=A< staged[0];
	let H;
	if B{
	let E=C+ (((A- C)- D).exp());
	H=E;
	}else{
	let G=A> (F- D);
	oG=G;
	let K=if G{
	let J=F- (((F- A)- D).exp());
	J
	}else{
	A
	};
	H=K;
	}
	let I=(H+ 273.15f64)- staged[1];
	let L=D+ (I* (staged[4]+ (I* staged[3])));
	let M=L< 0.11f64;
	let O=if M{
	let N=0.01f64+ (0.1f64* (((10f64* (L- 0.01f64))- D).exp()));
	N
	}else{
	L
	};
	let P=staged[5]* O;
    [B as u8 as f64, oG as u8 as f64, M as u8 as f64, P]
}


static CANONICAL_MODEL_CACHE: OnceLock<Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>>> = OnceLock::new();

fn canonical_model_cache() -> &'static Mutex<HashMap<Box<[u64]>, Weak<CanonicalModelValues>>> {
    CANONICAL_MODEL_CACHE.get_or_init(|| Mutex::new(HashMap::new()))
}

fn canonical_model_cache_lookup(key: &[u64]) -> Option<Arc<CanonicalModelValues>> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    let found = cache.get(key).and_then(Weak::upgrade);
    if found.is_none() {
        cache.remove(key);
    }
    found
}

fn canonical_model_cache_intern(
    key: Box<[u64]>,
    candidate: Arc<CanonicalModelValues>,
) -> Arc<CanonicalModelValues> {
    let mut cache = canonical_model_cache()
        .lock()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    if let Some(existing) = cache.get(key.as_ref()).and_then(Weak::upgrade) {
        return existing;
    }
    cache.retain(|_, values| values.strong_count() > 0);
    cache.insert(key, Arc::downgrade(&candidate));
    candidate
}

impl Instance {
    fn canonical_model_key(&self) -> Box<[u64]> {
        let mut key = Vec::with_capacity(72);
        for index in 0..Self::PARAMETER_COUNT {
            if PARAMETER_MODEL_FLAGS[index] {
                key.push(self.params.values[index].to_bits());
                key.push(u64::from(self.param_given[index]));
            }
        }
        key.into_boxed_slice()
    }

    fn canonical_install_model_values(&mut self, values: Arc<CanonicalModelValues>) {
        install_generated_stage_values(&mut self.canonical_staged[..], values.as_ref(), &CANONICAL_MODEL_STAGE_SLOTS);
        self.canonical_model_values = Some(values);
    }

    fn canonical_model_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_model_values.is_some() {
            return;
        }
        let key = self.canonical_model_key();
        if let Some(values) = canonical_model_cache_lookup(key.as_ref()) {
            self.canonical_install_model_values(values);
            return;
        }
        let produced: CanonicalModelValues = canonical_model_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        let values = canonical_model_cache_intern(key, Arc::new(produced));
        self.canonical_install_model_values(values);
    }

    fn canonical_instance_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        if self.canonical_instance_valid {
            return;
        }
        let produced = canonical_instance_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_INSTANCE_STAGE_SLOTS);
        self.canonical_instance_valid = true;
    }

    fn canonical_temperature_stage(&mut self, ctx: &GeneratedEvalContext<'_>) {
        let temperature = ctx.temperature();
        let thermal_voltage = ctx.thermal_voltage();
        if self.canonical_temperature_valid
            && self.canonical_temperature == temperature
            && self.canonical_thermal_voltage == thermal_voltage
        {
            return;
        }
        let produced = canonical_temperature_preprocess(
            &self.params.values,
            &self.param_given[..],
            self.multiplicity,
            &self.canonical_staged[..],
            ctx.temperature(),
            ctx.thermal_voltage(),
        );
        install_generated_stage_values(&mut self.canonical_staged[..], &produced, &CANONICAL_TEMPERATURE_STAGE_SLOTS);
        self.canonical_temperature = temperature;
        self.canonical_thermal_voltage = thermal_voltage;
        self.canonical_temperature_valid = true;
    }

    pub fn stamp(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedStamper<'_>) {
        self.canonical_model_stage(ctx);
        self.canonical_instance_stage(ctx);
        self.canonical_temperature_stage(ctx);
        let parameters = &self.params.values;
        let multiplicity = self.multiplicity;
        let staged = &*self.canonical_staged;
        let node_potentials = [ctx.node_voltage(self.nodes[0]), ctx.node_voltage(self.nodes[1])];
		let C=staged[29]!=0.0;
		let D=staged[7];
		let G=parameters[27];
		let J=1f64;
		let L=parameters[25];
		let R=0.3333333333333333f64;
		let S=parameters[28];
		let T=parameters[26];
		let W=L2([0f64;2]);
		let Z=staged[9];
		let AF=0.0;
		let AG=0.0;
		let A=node_potentials[0]- node_potentials[1];
		let B=L2([1f64,0.0])- L2([0.0,1f64]);
		let X;
		let Y;
		if C{
		let E=A/ D;
		let F=B/ D;
		let H=G* E;
		let I=(F* G)* H;
		let K=(J+ (H* H)).sqrt();
		let M=L* (E.abs());
		let N=(F* ((2f64* ((E>= 0f64) as u8 as f64))- 1f64))* L;
		let O=M* M;
		let P=N* M;
		let Q=J+ (O* M);
		let U=(staged[8]+ (S* K))+ (T* (Q.powf(R)));
		let V=(((I+ I)* (1f64/ (2f64* K)))* S)+ (((((P+ P)* M)+ (N* O))* (R* (Q.powf(-0.6666666666666667f64))))* T);
		X=U;
		Y=V;
		}else{
		X=J;
		Y=W;
		}
		let AA=Z* X;
		let AB=A/ AA;
		let AC=(B- ((Y* Z)* AB))/ AA;
		let AD=AC[0];
		let AE=AC[1];
        stamper.stamp_current_sparse_local::<2, 0>(
            Some(0),
            Some(1),
            multiplicity * (AB),
            [0, 1],
            [AD, AE],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (AF),
            [],
            [],
            [],
            [],
            multiplicity,
        );
        stamper.stamp_current_sparse_local::<0, 0>(
            Some(0),
            Some(1),
            multiplicity * (AG),
            [],
            [],
            [],
            [],
            multiplicity,
        );
    }

    pub fn stamp_reactive(&mut self, ctx: &GeneratedEvalContext<'_>, stamper: &mut GeneratedReactiveStamper<'_>) {
    }

}
