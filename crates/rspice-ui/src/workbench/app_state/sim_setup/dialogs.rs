//! Analysis setup dialogs.
//!
//! The options sheet behind the Simulate workspace, and the plan palette
//! that switches between stored analysis plans.

use crate::workbench::app::RSpiceApp;

pub(in crate::workbench) mod options_dialog;
